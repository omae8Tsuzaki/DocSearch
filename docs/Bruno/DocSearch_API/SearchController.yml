info:
  name: SearchController
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/api/search?q=test"
  auth: inherit
  params:
    - name: q
      value: test
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
