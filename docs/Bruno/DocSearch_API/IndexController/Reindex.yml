info:
  name: Reindex
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/index/reindex"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
