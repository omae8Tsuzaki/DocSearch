info:
  name: BrowseController
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/browse"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
